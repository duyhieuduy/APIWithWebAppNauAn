@model IEnumerable<APIWithWeb.Models.Nguyenlieu>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tennguyenlieu)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Anhnguyenlieu)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tennguyenlieu)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Anhnguyenlieu)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Manguyenlieu">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Manguyenlieu">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Manguyenlieu">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
